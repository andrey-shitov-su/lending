# shitov.su — личный сайт Andrey.Shitov

Статический одностраничник без бэкенда. Хостинг — GitHub Pages, домен `shitov.su`
(файл `CNAME`). Сборки нет: `index.html` открывается как есть.

## Структура

```
index.html   вся страница: разметка, стили и скрипты в одном файле
assets/      logo.svg, logo.png, photo.jpg, og.jpg
certs/       сканы документов об образовании (1600 px, до 500 КБ)
CNAME        кастомный домен для GitHub Pages
```

## Разделы

Hero · Обо мне · Стек · Проекты · Образование · YouTube · Для HR · Контакты

## Как править контент

Проекты и документы лежат в двух массивах в конце `index.html`:

- `const projects` — карточки раздела «Проекты». Поля: `cat` (фильтр), `org`, `title`,
  `short` (текст карточки), `desc`, `role`, `year`, `res` (измеримый результат),
  `list` (что сделано), `tags`.
- `const credentials` — карточки раздела «Образование». Поля: `type` (`diploma` / `cert`
  / `award`), `title`, `issuer`, `year`, `note`, `file` (путь к скану),
  `verify` (ссылка на проверку подлинности, необязательно).

Цвета — четыре блока токенов в начале файла: `:root`, `[data-theme="dark"]`,
`[data-brand="redhat"]`, `[data-brand="redhat"][data-theme="dark"]`. Правьте токен,
а не отдельные правила.

## Локальный просмотр

```
python3 -m http.server 8123
```

## Что ещё не сделано

- DNS: A-записи `shitov.su` на 185.199.108.153 / 109.153 / 110.153 / 111.153
  и включение GitHub Pages в настройках репозитория.
- 30 августа 2026 — заменить тизер в разделе YouTube на ссылку на вышедшее видео.
  Обратный отсчёт до этой даты считается от константы `RELEASE` в скрипте; после
  30 августа он сам перестаёт показываться, но текст тизера надо поменять руками.
- Раздел «Экспертиза» из плана пока свёрнут в блок «Стек»; расширение — этап 3.
- Разделы «Схемы» (C4/EPC) и «Курсы» сняты со страницы.
- Фото: 960 px по короткой стороне, целевой минимум — 1200 px. Для финальной версии переснять.
